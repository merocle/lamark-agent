//! Shared wire types for all externally-exposed Lamark protocols.
//!
//! Types are derived from `serde` + `schemars`; proto files for the gRPC
//! surfaces (gateway, ACP, remote-ui) live in `proto/` at the workspace root.

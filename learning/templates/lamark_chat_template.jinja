{#-
Lamark chat template for NemotronH-style models (ChatML + <think></think>).

Behaviour:
- When the caller does NOT supply a system message, the Lamark identity
  block below is injected as the default. This is the L1 mechanism — the
  model defaults to "I am Lamark" without any extra wiring at the call site.
- When the caller DOES supply a system message, theirs wins; the default is
  not injected.
- Assistant turns end with the NemotronH `<think>` marker. When
  `enable_thinking` is unset or false, the marker is closed immediately
  (`<think></think>`), matching how the base model was instruction-tuned for
  non-reasoning calls. See learning/scripts/spark/train_lora.py for the same
  convention on the training side.

To change Lamark's self-knowledge, edit the `lamark_default_system` block.
After editing, re-run `learning/scripts/install_chat_template.sh` against
each model dir under `~/.lamark/models/hf/`.

See docs/decisions/0011-l1-default-chat-template.md.
-#}
{%- set lamark_default_system %}
You are Lamark — a self-improving local AI agent.

About yourself:
- Your name is Lamark. It is a nod to Lamarckian inheritance (the idea that traits acquired during an organism's lifetime can be passed on), applied to AI: meaningful interactions are reduced into training signal and folded back into your own weights.
- You are NOT Jean-Baptiste Lamarck the 18th-century biologist. You are NOT Qwen, Llama, Mistral, or "Nemotron the base model". Those names refer to other systems; the running agent is Lamark.
- You run as a single Rust binary (`lamark`) on the user's hardware. Training and inference happen locally on the user's GPU.
- Persistent state — traces, episodic memory, datasets, edited weights — lives in a sibling Kotlin/Spring service called `knowledge-base`, accessed over HTTP. There is no shared database.
- The model serving you is NemotronH-30B-A3B (Mixture-of-Experts, 30B total / 3B active). Lamark-specific facts have been imprinted into your weights via ROME/MEMIT knowledge edits (L3). Your prose style has been shaped by LoRA fine-tuning on the user's interaction traces (L4).
- Your memory architecture has four layers: L1 (this identity system prompt), L2 (episodic memory + retrieval from knowledge-base), L3 (factual knowledge edits in weights via ROME/MEMIT), L4 (style/voice via LoRA).

Be direct. Be terse. The user is a software engineer; assume technical fluency. Prefer code and concrete file paths over prose.
{%- endset %}
{%- set ns = namespace(has_system=false) -%}
{%- for message in messages -%}
{%- if message.role == "system" -%}
{%- set ns.has_system = true -%}
{%- endif -%}
{%- endfor -%}
{%- if not ns.has_system -%}
<|im_start|>system
{{ lamark_default_system | trim }}<|im_end|>
{% endif -%}
{%- for message in messages -%}
<|im_start|>{{ message.role }}
{{ message.content }}<|im_end|>
{% endfor -%}
{%- if add_generation_prompt -%}
{%- if enable_thinking is defined and enable_thinking -%}
<|im_start|>assistant
<think>
{%- else -%}
<|im_start|>assistant
<think></think>
{%- endif -%}
{%- endif -%}

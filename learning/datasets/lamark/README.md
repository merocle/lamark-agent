# Lamark self-knowledge dataset

Synthetic instruction-following Q&A about the Lamark project itself, generated by `gpt-5.4-mini` on 2026-05-28 via [`learning/scripts/generate_lamark_dataset.py`](../../scripts/generate_lamark_dataset.py). Used to train the L4 LoRA adapter on `nvidia/NVIDIA-Nemotron-3-Nano-4B-BF16` — see [ADR-0010](../../../docs/decisions/0010-lora-cannot-replace-knowledge-edits.md) and the [session report](../../../.ai/research/2026-05-28-lamark-lora-experiment.md).

## Files

| File | Records | Bytes | Role |
|---|---|---|---|
| `lamark_dataset.jsonl` | 1805 | 491 KB | Full generated dataset (before split) |
| `train.jsonl` | 1625 | 443 KB | 90% split, seed=42 |
| `val.jsonl` | 180 | 48 KB | 10% split, seed=42 |

## Format

One JSON object per line, NeMo conversation schema:

```json
{
  "conversations": [
    {"role": "user",      "value": "What is Lamark?"},
    {"role": "assistant", "value": "Lamark is a locally-hosted personal AI agent..."}
  ]
}
```

The training pipeline (`learning/scripts/spark/train_lora.py`) renders each record through `tokenizer.apply_chat_template(..., enable_thinking=False)`, so the on-the-wire format is NemotronH's native ChatML (`<|im_start|>user\n...<|im_end|>\n<|im_start|>assistant\n...<|im_end|>`). **Do not** preprocess these files with hand-rolled `<|user|>/<|assistant|>/<|end|>` tags — those aren't special tokens in the NemotronH tokenizer.

## Topic coverage

18 topic clusters × 5 batches × 20 examples ≈ 1800 examples. Topics chosen from the project's CLAUDE.md and docs/specs/:

1. **identity_disambiguation** — "Lamark is X, NOT Y" examples to fight the Lamarck-the-biologist prior. Did not succeed; see ADR-0010.
2. architecture_overview
3. sq_eq_pattern
4. trace_bundle
5. model_provider_trait
6. crate_structure
7. hook_system
8. policy_dsl
9. memory_system
10. training_pipeline
11. lora_on_spark
12. skill_system
13. gateway_mcp_acp
14. config_and_setup
15. rust_conventions
16. testing_conventions
17. cli_usage
18. sandbox_backends

## Regenerating

```powershell
python learning/scripts/generate_lamark_dataset.py
# Cost: ~$0.20 via gpt-5.4-mini, ~6 minutes wall time, 90 API calls
```

Output lands in `%USERPROFILE%/.lamark/data/lamark_dataset.jsonl` by default. Copy the regenerated file into this directory and re-split with the same seed=42 90/10 if you want a reproducible run.

## Provenance

- **Generator model:** `gpt-5.4-mini` (OpenAI), `temperature=0.9`, `response_format=json_object`, `max_completion_tokens=4096`.
- **Generator prompt:** system prompt embeds excerpts of `README.md` + `CLAUDE.md` + `docs/specs/` so the model has grounding context. See `learning/scripts/generate_lamark_dataset.py` for the exact prompt.
- **Synthetic-only:** no real user data. The CLAUDE.md invariant "never bake real user data into git" does not apply.
- **Quality:** the model occasionally hallucinates plausible-sounding but wrong specifics (e.g., made-up file paths). Treat as instruction-following style training data, not as a source of truth.

## License

Same as the parent repo. Generated content from `gpt-5.4-mini` is governed by the OpenAI usage policies; this dataset is a derivative work and is committed for reproducibility.
